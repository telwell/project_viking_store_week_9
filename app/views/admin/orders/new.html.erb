<h2>Create New Order for <%= "#{@user.first_name} #{@user.last_name}" %>:</h2>

<%= render partial: "admin/shared/errors", :locals => { :error_object => @order } %>

<%= render partial: "order_form" %>

<p class="col-sm-12 text-center">You can add products on the next screen,<br/> once order has been created.</p>